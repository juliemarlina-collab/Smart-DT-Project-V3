# Smart DT Project V3

Polished working build for GitHub Pages.

## Live flow
index → welcome → login → dashboard → phase pages → progress/profile.

## Included fixes
- Cloudinary asset paths applied to main pages.
- `app.js` loads before `data.js`, `ui.js`, and `phase-engine.js`.
- Phase 05 includes T14, T15 and T16 templates.
- Rule-based DT Coach enabled on phase pages without exposing any API key.
- Template autosave supports prefixed and unprefixed field names, checkboxes, and radios.
- Hero and visual layout restored for a more polished mobile app presentation.

## Notes
Separate `phaseXX-templates` files are kept in `_template_sources_backup` only as references. The intended student flow is inside each phase page: Quick Info → Quiz → Templates → Submit.
